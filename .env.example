# Unipile API credentials
# These are typically passed via MCP registration config (not loaded at runtime from .env)
# Reference only — copy to .env if running outside MCP context

UNIPILE_API_KEY=your-api-key-here
UNIPILE_DSN=apiXX.unipile.com:XXXXX
